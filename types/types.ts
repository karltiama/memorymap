export type MemoryData = {
  id: string;
  title: string;
  description: string;
  latitude: number;
  longitude: number;
  image_url?: string;
  address?: string;
  tags?: string[];
  // Add other fields as necessary
};
